#ifndef UTILS_H
#define UTILS_H
enum class TType
{
    
	I,
    O,
    L,
    S,
    Z,
	T,
    J,
    R,
    Q,
    Def
};

enum class Rotation{
    LEFT,
    RIGHT
};

enum {
    FREE, FILLED, BORDER
};

enum class RelativePosition {
    TOP,
    LEFT,
    BOTTOM,
    RIGHT
};

#endif